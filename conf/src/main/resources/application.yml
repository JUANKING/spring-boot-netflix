server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/JUANKING/boot
  application:
    name: configuracion
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:3000/eureka/